# Plan 2: College B (Oracle) + College C (MySQL) 垂直切片 + XSL 转换层

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 College B（Oracle）和 College C（MySQL）的端到端垂直切片（DAO→Server→Client），以及 Integration Server 的 12 个 XSL 格式转换文件。结束时可演示 B/C 客户端登录、查本院课、选/退本院课。

**Architecture:** 参照 College A 的成熟模式（JDBC→DAO→Adapter→Handler→Server），B 用 Oracle Docker 容器，C 用 MySQL Docker 容器。XSL 文件放置于 `integration/src/main/resources/xsl/`，实现三院格式 ↔ 统一格式的双向转换。

**Tech Stack:** Java 17, Maven, DOM4J, Xerces, XSLT/TrAX, ojdbc8 (Oracle), mysql-connector-j (MySQL), Swing。

**前置条件:** Plan 1 全部完成；Docker 可用；`mvn install` 通过。

**参考:** 设计 spec `docs/superpowers/specs/2026-05-26-xml-data-integration-design.md` §4.1（异构表结构）、§4.3（XSD/XSL 清单）、§9（种子数据）。

---

## Task 1: Oracle Docker 启动脚本 + B 院 init_b.sql

**Files:**
- Create: `scripts/db/start-oracle.sh`
- Create: `college-b/src/main/resources/sql/init_b.sql`
- Create: `college-b/src/main/resources/db.properties`

Oracle 表结构按 design spec §4.1：

| 表 | 字段 |
|---|---|
| 账户 | `账户名 varchar2(12)` PK / `密码 varchar2(12)` / `级别 number(2)` / `客体 varchar2(9)` FK→学生.学号 |
| 学生 | `学号 varchar2(12)` PK / `姓名 varchar2(10)` / `性别 varchar2(2)` / `专业 varchar2(10)` / `密码 varchar2(12)` |
| 课程 | `编号 varchar2(5)` PK / `名称 varchar2(20)` / `课时 number` / `学分 number(3,1)` / `老师 varchar2(10)` / `地点 varchar2(20)` / `共享 char(1)` |
| 选课 | `课程编号 varchar2(5)` PK / `学号 varchar2(12)` / `得分 varchar2(3)` |

注意 key differences from A：
- 学生表无"院系"字段，有"专业"和"密码"
- 账户表多"级别"字段和"客体"FK，无"权限"字段
- 课程编号是 `varchar2(5)` 不是 `varchar(8)`
- 选课表无复合主键/唯一约束（课件原文如此），无"来源"字段

- [ ] **Step 1: 写 Oracle Docker 启动脚本**

```bash
#!/usr/bin/env bash
# scripts/db/start-oracle.sh
set -e
NAME=di-oracle
PASSWORD='oracle123'

if docker ps -a --format '{{.Names}}' | grep -q "^${NAME}$"; then
  docker start "${NAME}"
else
  docker run -e "ORACLE_PASSWORD=${PASSWORD}" \
    -p 1521:1521 --name "${NAME}" \
    -d gvenzl/oracle-free:23-slim
fi

echo "Waiting for Oracle..."
for i in {1..60}; do
  docker exec "${NAME}" sqlplus -s system/"${PASSWORD}"@freepdb1 <<< "SELECT 1 FROM DUAL" >/dev/null 2>&1 && break
  sleep 3
done
echo "Oracle up at localhost:1521 (system / ${PASSWORD}, service=freepdb1)"
```

```bash
chmod +x scripts/db/start-oracle.sh
```

- [ ] **Step 2: 写 init_b.sql**

```sql
-- college-b/src/main/resources/sql/init_b.sql

-- Create user
ALTER SESSION SET CONTAINER=freepdb1;
CREATE USER collegeb IDENTIFIED BY collegeb;
GRANT CONNECT, RESOURCE, UNLIMITED TABLESPACE TO collegeb;

CONNECT collegeb/collegeb@freepdb1;

-- Drop existing
DROP TABLE 选课 CASCADE CONSTRAINTS;
DROP TABLE 学生 CASCADE CONSTRAINTS;
DROP TABLE 课程 CASCADE CONSTRAINTS;
DROP TABLE 账户 CASCADE CONSTRAINTS;

CREATE TABLE 账户 (
  账户名 varchar2(12) PRIMARY KEY,
  密码   varchar2(12) NOT NULL,
  级别   number(2)    NOT NULL,
  客体   varchar2(9)
);

CREATE TABLE 学生 (
  学号 varchar2(12) PRIMARY KEY,
  姓名 varchar2(10) NOT NULL,
  性别 varchar2(2)  NOT NULL,
  专业 varchar2(10) NOT NULL,
  密码 varchar2(12) NOT NULL
);

ALTER TABLE 账户 ADD CONSTRAINT FK_账户_客体 FOREIGN KEY (客体) REFERENCES 学生(学号);

CREATE TABLE 课程 (
  编号 varchar2(5)   PRIMARY KEY,
  名称 varchar2(20)  NOT NULL,
  课时 number         NOT NULL,
  学分 number(3,1)    NOT NULL,
  老师 varchar2(10)  NOT NULL,
  地点 varchar2(20)  NOT NULL,
  共享 char(1)       NOT NULL
);

CREATE TABLE 选课 (
  课程编号 varchar2(5)  NOT NULL REFERENCES 课程(编号),
  学号     varchar2(12) NOT NULL REFERENCES 学生(学号),
  得分     varchar2(3)
);
```

- [ ] **Step 3: 写 db.properties**

```properties
# college-b/src/main/resources/db.properties
jdbc.url=jdbc:oracle:thin:@//localhost:1521/freepdb1
jdbc.user=collegeb
jdbc.password=collegeb
```

- [ ] **Step 4: 更新 college-b/pom.xml 加 Oracle JDBC 驱动**

```xml
<!-- 在 college-b/pom.xml 的 dependencies 中追加 -->
<dependency>
  <groupId>com.oracle.database.jdbc</groupId>
  <artifactId>ojdbc8</artifactId>
  <version>21.9.0.0</version>
</dependency>
```

同时加到父 pom `<dependencyManagement>` 中（方便 C 也可以用）。

- [ ] **Step 5: 启动 Oracle 并执行建库**

```bash
./scripts/db/start-oracle.sh
# 等 Oracle 就绪后执行建库
docker exec -i di-oracle sqlplus -s system/oracle123@freepdb1 < college-b/src/main/resources/sql/init_b.sql
```

预期无报错。

- [ ] **Step 6: 验证编译**

```bash
mvn -q -DskipTests compile
```

预期 BUILD SUCCESS。

- [ ] **Step 7: commit**

```bash
git add scripts/db/start-oracle.sh college-b/src/main/resources/sql/init_b.sql college-b/src/main/resources/db.properties college-b/pom.xml pom.xml
git commit -m "build(college-b): oracle docker script, init schema, and jdbc driver"
```

---

## Task 2: B 院 JdbcFactory（Oracle 适配）

**Files:**
- Create: `college-b/src/main/java/college/b/jdbc/JdbcFactory.java`

参照 College A 的 JdbcFactory，适配 Oracle 连接。

- [ ] **Step 1: 实现**

```java
// college-b/src/main/java/college/b/jdbc/JdbcFactory.java
package college.b.jdbc;

import javax.sql.DataSource;
import java.io.IOException;
import java.io.PrintWriter;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.util.Properties;
import java.util.logging.Logger;

public final class JdbcFactory {
  private JdbcFactory() {}

  public static DataSource fromClasspath(String resource) throws IOException {
    Properties p = new Properties();
    try (var in = JdbcFactory.class.getResourceAsStream(resource)) {
      if (in == null) throw new IOException("resource not found: " + resource);
      p.load(in);
    }
    String url = p.getProperty("jdbc.url");
    String user = p.getProperty("jdbc.user");
    String pass = p.getProperty("jdbc.password");
    return new SimpleDataSource(url, user, pass);
  }

  private record SimpleDataSource(String url, String user, String pass) implements DataSource {
    @Override public Connection getConnection() throws SQLException {
      return DriverManager.getConnection(url, user, pass);
    }
    @Override public Connection getConnection(String u, String p) throws SQLException {
      return DriverManager.getConnection(url, u, p);
    }
    @Override public PrintWriter getLogWriter() { return null; }
    @Override public void setLogWriter(PrintWriter out) {}
    @Override public void setLoginTimeout(int seconds) {}
    @Override public int getLoginTimeout() { return 0; }
    @Override public Logger getParentLogger() { return Logger.getLogger("jdbc"); }
    @SuppressWarnings("unchecked")
    @Override public <T> T unwrap(Class<T> i) { throw new UnsupportedOperationException(); }
    @Override public boolean isWrapperFor(Class<?> i) { return false; }
  }
}
```

- [ ] **Step 2: 验证编译**

```bash
mvn -q -DskipTests compile
```

- [ ] **Step 3: commit**

```bash
git add college-b/src/main/java/
git commit -m "feat(college-b): jdbc factory for oracle connections"
```

---

## Task 3: B 院 AccountDao（Oracle）

**Files:**
- Create: `college-b/src/main/java/college/b/dao/AccountDao.java`
- Test: `college-b/src/test/java/college/b/dao/AccountDaoIT.java`

B 院账户表结构与 A 不同：`账户名`/`密码`/`级别`/`客体`，无"权限"字段。AuthService 只需绑定账户名→密码+级别（作为 role），客体 FK 暂不校验。

- [ ] **Step 1: 写集成测试（FAIL）**

```java
// college-b/src/test/java/college/b/dao/AccountDaoIT.java
package college.b.dao;

import college.b.jdbc.JdbcFactory;
import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;

@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
class AccountDaoIT {
  static AccountDao dao;

  @BeforeAll
  static void setup() throws Exception {
    var ds = JdbcFactory.fromClasspath("/db.properties");
    dao = new AccountDao(ds);
    try (var c = ds.getConnection(); var st = c.createStatement()) {
      st.execute("DELETE FROM 账户 WHERE 账户名='unit_test'");
      st.execute("INSERT INTO 账户(账户名,密码,级别) VALUES('unit_test','pwd123',1)");
      c.commit();
    }
  }

  @Test @Order(1)
  void findByUsername_returns_account() {
    var acc = dao.findByUsername("unit_test").orElseThrow();
    assertEquals("pwd123", acc.password());
    assertEquals(1, acc.level());
  }

  @Test @Order(2)
  void findByUsername_unknown_returns_empty() {
    assertTrue(dao.findByUsername("__nope__").isEmpty());
  }

  @AfterAll
  static void teardown() throws Exception {
    try (var c = JdbcFactory.fromClasspath("/db.properties").getConnection();
         var st = c.createStatement()) {
      st.execute("DELETE FROM 账户 WHERE 账户名='unit_test'");
      c.commit();
    }
  }
}
```

- [ ] **Step 2: 运行确认 FAIL**

```bash
mvn -pl college-b -am test -Dtest=AccountDaoIT
```
预期：编译失败（AccountDao 不存在）。

- [ ] **Step 3: 实现 AccountDao**

```java
// college-b/src/main/java/college/b/dao/AccountDao.java
package college.b.dao;

import javax.sql.DataSource;
import java.sql.SQLException;
import java.util.Optional;

public class AccountDao {
  public record Account(String username, String password, int level, String objectRef) {}

  private final DataSource ds;

  public AccountDao(DataSource ds) { this.ds = ds; }

  public Optional<Account> findByUsername(String username) {
    String sql = "SELECT 账户名,密码,级别,客体 FROM 账户 WHERE 账户名=?";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, username);
      try (var rs = ps.executeQuery()) {
        if (!rs.next()) return Optional.empty();
        return Optional.of(new Account(
            rs.getString(1), rs.getString(2), rs.getInt(3), rs.getString(4)));
      }
    } catch (SQLException e) {
      throw new RuntimeException("query account failed", e);
    }
  }
}
```

- [ ] **Step 4: 运行测试确认 PASS**

```bash
mvn -pl college-b -am test -Dtest=AccountDaoIT
```

- [ ] **Step 5: commit**

```bash
git add college-b/src/
git commit -m "feat(college-b): account dao for oracle (level-based auth)"
```

---

## Task 4: B 院 AuthService

**Files:**
- Create: `college-b/src/main/java/college/b/service/AuthService.java`
- Test: `college-b/src/test/java/college/b/service/AuthServiceTest.java`

B 院用 level（0-9）作为权限标记：`>=1` 视为学生，`>=5` 视为管理员。登录成功返回 `Session(username, role)`。

- [ ] **Step 1: 写单元测试（FAIL）**

```java
// college-b/src/test/java/college/b/service/AuthServiceTest.java
package college.b.service;

import college.b.dao.AccountDao;
import org.junit.jupiter.api.Test;
import java.util.Optional;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

class AuthServiceTest {
  @Test
  void login_success() {
    var dao = mock(AccountDao.class);
    when(dao.findByUsername("u")).thenReturn(
        Optional.of(new AccountDao.Account("u", "p", 1, null)));
    var svc = new AuthService(dao);
    var s = svc.login("u", "p").orElseThrow();
    assertEquals("u", s.username());
    assertEquals("stu", s.role());
  }

  @Test
  void login_admin() {
    var dao = mock(AccountDao.class);
    when(dao.findByUsername("u")).thenReturn(
        Optional.of(new AccountDao.Account("u", "p", 5, null)));
    var s = new AuthService(dao).login("u", "p").orElseThrow();
    assertEquals("adm", s.role());
  }

  @Test
  void login_wrong_password_empty() {
    var dao = mock(AccountDao.class);
    when(dao.findByUsername("u")).thenReturn(
        Optional.of(new AccountDao.Account("u", "p", 1, null)));
    assertTrue(new AuthService(dao).login("u", "x").isEmpty());
  }

  @Test
  void login_unknown_empty() {
    var dao = mock(AccountDao.class);
    when(dao.findByUsername("x")).thenReturn(Optional.empty());
    assertTrue(new AuthService(dao).login("x", "p").isEmpty());
  }
}
```

- [ ] **Step 2: 实现 AuthService**

```java
// college-b/src/main/java/college/b/service/AuthService.java
package college.b.service;

import college.b.dao.AccountDao;
import java.util.Optional;

public class AuthService {
  public record Session(String username, String role) {}

  private final AccountDao dao;

  public AuthService(AccountDao dao) { this.dao = dao; }

  public Optional<Session> login(String user, String pass) {
    return dao.findByUsername(user)
        .filter(a -> a.password().equals(pass))
        .map(a -> new Session(a.username(), a.level() >= 5 ? "adm" : "stu"));
  }
}
```

- [ ] **Step 3: 运行测试确认 PASS**

```bash
mvn -pl college-b -am test -Dtest=AuthServiceTest
```

- [ ] **Step 4: commit**

```bash
git add college-b/src/
git commit -m "feat(college-b): auth service with level-based role mapping"
```

---

## Task 5: B 院 StudentDao（Oracle）

**Files:**
- Create: `college-b/src/main/java/college/b/dao/StudentDao.java`
- Test: `college-b/src/test/java/college/b/dao/StudentDaoIT.java`

B 学生表字段：`学号`/`姓名`/`性别`/`专业`/`密码`。注意与 A 的差异：有"专业" / "密码"，无"院系" / "关联账户"。

- [ ] **Step 1: 写集成测试（FAIL）**

```java
// college-b/src/test/java/college/b/dao/StudentDaoIT.java
package college.b.dao;

import college.b.jdbc.JdbcFactory;
import org.junit.jupiter.api.*;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;

@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
class StudentDaoIT {
  static StudentDao dao;

  @BeforeAll
  static void setup() throws Exception {
    dao = new StudentDao(JdbcFactory.fromClasspath("/db.properties"));
    try (var c = JdbcFactory.fromClasspath("/db.properties").getConnection();
         var st = c.createStatement()) {
      st.execute("DELETE FROM 学生 WHERE 学号 LIKE 'IT_%'");
      st.execute("INSERT INTO 学生(学号,姓名,性别,专业,密码) VALUES('IT_001','测试甲','男','计算机','pw1')");
      st.execute("INSERT INTO 学生(学号,姓名,性别,专业,密码) VALUES('IT_002','测试乙','女','数学','pw2')");
      c.commit();
    }
  }

  @AfterAll
  static void teardown() throws Exception {
    try (var c = JdbcFactory.fromClasspath("/db.properties").getConnection();
         var st = c.createStatement()) {
      st.execute("DELETE FROM 学生 WHERE 学号 LIKE 'IT_%'");
      c.commit();
    }
  }

  @Test @Order(1)
  void findById_present() {
    var s = dao.findById("IT_001").orElseThrow();
    assertEquals("测试甲", s.name());
    assertEquals("计算机", s.major());
  }

  @Test @Order(2)
  void findAll_includes_inserted() {
    List<StudentDao.Row> all = dao.findAll();
    assertTrue(all.stream().anyMatch(r -> r.id().equals("IT_002")));
  }

  @Test @Order(3)
  void insertIfMissing_inserts_once() {
    boolean first = dao.insertIfMissing(new StudentDao.Row("IT_003", "测试丙", "男", "物理", "pw3"));
    boolean second = dao.insertIfMissing(new StudentDao.Row("IT_003", "测试丙", "男", "物理", "pw3"));
    assertTrue(first);
    assertFalse(second);
  }
}
```

- [ ] **Step 2: 实现 StudentDao**

```java
// college-b/src/main/java/college/b/dao/StudentDao.java
package college.b.dao;

import javax.sql.DataSource;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

public class StudentDao {
  public record Row(String id, String name, String sex, String major, String password) {}

  private final DataSource ds;

  public StudentDao(DataSource ds) { this.ds = ds; }

  public Optional<Row> findById(String id) {
    String sql = "SELECT 学号,姓名,性别,专业,密码 FROM 学生 WHERE 学号=?";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, id);
      try (var rs = ps.executeQuery()) { return rs.next() ? Optional.of(map(rs)) : Optional.empty(); }
    } catch (SQLException e) { throw new RuntimeException(e); }
  }

  public List<Row> findAll() {
    String sql = "SELECT 学号,姓名,性别,专业,密码 FROM 学生";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql); var rs = ps.executeQuery()) {
      List<Row> out = new ArrayList<>();
      while (rs.next()) out.add(map(rs));
      return out;
    } catch (SQLException e) { throw new RuntimeException(e); }
  }

  public boolean insertIfMissing(Row r) {
    String sql = "INSERT INTO 学生(学号,姓名,性别,专业,密码) "
               + "SELECT ?,?,?,?,? FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM 学生 WHERE 学号=?)";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, r.id()); ps.setString(2, r.name());
      ps.setString(3, r.sex()); ps.setString(4, r.major());
      ps.setString(5, r.password()); ps.setString(6, r.id());
      int rows = ps.executeUpdate();
      c.commit();
      return rows == 1;
    } catch (SQLException e) { throw new RuntimeException(e); }
  }

  private static Row map(java.sql.ResultSet rs) throws SQLException {
    return new Row(rs.getString(1), rs.getString(2), rs.getString(3), rs.getString(4), rs.getString(5));
  }
}
```

- [ ] **Step 3: 运行测试确认 PASS**

```bash
mvn -pl college-b -am test -Dtest=StudentDaoIT
```

- [ ] **Step 4: commit**

```bash
git add college-b/src/
git commit -m "feat(college-b): student dao for oracle"
```

---

## Task 6: B 院 CourseDao（Oracle）

**Files:**
- Create: `college-b/src/main/java/college/b/dao/CourseDao.java`
- Test: `college-b/src/test/java/college/b/dao/CourseDaoIT.java`

B 院课程表字段：`编号`/`名称`/`课时`/`学分`/`老师`/`地点`/`共享`。

- [ ] **Step 1: 写集成测试 + 实现 + 测试 + commit**

```java
// college-b/src/test/java/college/b/dao/CourseDaoIT.java
package college.b.dao;

import college.b.jdbc.JdbcFactory;
import org.junit.jupiter.api.*;
import java.math.BigDecimal;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;

@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
class CourseDaoIT {
  static CourseDao dao;

  @BeforeAll
  static void setup() throws Exception {
    dao = new CourseDao(JdbcFactory.fromClasspath("/db.properties"));
    try (var c = JdbcFactory.fromClasspath("/db.properties").getConnection();
         var st = c.createStatement()) {
      st.execute("DELETE FROM 课程 WHERE 编号 LIKE 'IT_%'");
      st.execute("INSERT INTO 课程(编号,名称,课时,学分,老师,地点,共享) VALUES('ITC01','测试课甲',32,2,'李老师','B101','Y')");
      st.execute("INSERT INTO 课程(编号,名称,课时,学分,老师,地点,共享) VALUES('ITC02','测试课乙',48,3,'王老师','B102','N')");
      c.commit();
    }
  }

  @AfterAll
  static void teardown() throws Exception {
    try (var c = JdbcFactory.fromClasspath("/db.properties").getConnection();
         var st = c.createStatement()) {
      st.execute("DELETE FROM 课程 WHERE 编号 LIKE 'IT_%'");
      c.commit();
    }
  }

  @Test @Order(1)
  void findById() {
    var c = dao.findById("ITC01").orElseThrow();
    assertEquals("测试课甲", c.name());
    assertEquals(0, BigDecimal.valueOf(2).compareTo(c.score()));
    assertTrue(c.shared());
  }

  @Test @Order(2)
  void findShared_only_Y() {
    List<CourseDao.Row> shared = dao.findShared();
    assertTrue(shared.stream().anyMatch(r -> r.id().equals("ITC01")));
    assertTrue(shared.stream().noneMatch(r -> r.id().equals("ITC02")));
  }
}
```

CourseDao 实现时注意：B 院课程字段名与 A 不同（`编号` vs `课程编号`，`老师` vs `授课老师`，`地点` vs `授课地点`）。Row record 用统一字段名（id, name, hours, score, teacher, location, shared）。

- [ ] **Step 2: 实现 CourseDao**

```java
// college-b/src/main/java/college/b/dao/CourseDao.java
package college.b.dao;

import javax.sql.DataSource;
import java.math.BigDecimal;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

public class CourseDao {
  public record Row(String id, String name, int hours, BigDecimal score,
                    String teacher, String location, boolean shared) {}

  private final DataSource ds;

  public CourseDao(DataSource ds) { this.ds = ds; }

  public Optional<Row> findById(String id) {
    String sql = "SELECT 编号,名称,课时,学分,老师,地点,共享 FROM 课程 WHERE 编号=?";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, id);
      try (var rs = ps.executeQuery()) { return rs.next() ? Optional.of(map(rs)) : Optional.empty(); }
    } catch (SQLException e) { throw new RuntimeException(e); }
  }

  public List<Row> findAll() {
    return query("SELECT 编号,名称,课时,学分,老师,地点,共享 FROM 课程");
  }

  public List<Row> findShared() {
    return query("SELECT 编号,名称,课时,学分,老师,地点,共享 FROM 课程 WHERE 共享='Y'");
  }

  private List<Row> query(String sql) {
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql); var rs = ps.executeQuery()) {
      List<Row> out = new ArrayList<>();
      while (rs.next()) out.add(map(rs));
      return out;
    } catch (SQLException e) { throw new RuntimeException(e); }
  }

  private static Row map(java.sql.ResultSet rs) throws SQLException {
    return new Row(rs.getString(1), rs.getString(2), rs.getInt(3),
                   rs.getBigDecimal(4), rs.getString(5), rs.getString(6),
                   "Y".equalsIgnoreCase(rs.getString(7)));
  }
}
```

- [ ] **Step 3: 运行测试 + commit**

```bash
mvn -pl college-b -am test -Dtest=CourseDaoIT
git add college-b/src/
git commit -m "feat(college-b): course dao for oracle"
```

---

## Task 7: B 院 ChoiceDao（Oracle）

**Files:**
- Create: `college-b/src/main/java/college/b/dao/ChoiceDao.java`
- Test: `college-b/src/test/java/college/b/dao/ChoiceDaoIT.java`

B 院选课表：`课程编号`/`学号`/`得分`，无复合唯一约束（按课件原文），无"来源"列。

- [ ] **Step 1: 写集成测试（FAIL）**

```java
// college-b/src/test/java/college/b/dao/ChoiceDaoIT.java
package college.b.dao;

import college.b.jdbc.JdbcFactory;
import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;

@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
class ChoiceDaoIT {
  static ChoiceDao dao;

  @BeforeAll
  static void setup() throws Exception {
    dao = new ChoiceDao(JdbcFactory.fromClasspath("/db.properties"));
    try (var c = JdbcFactory.fromClasspath("/db.properties").getConnection();
         var st = c.createStatement()) {
      st.execute("DELETE FROM 选课 WHERE 学号='IT_S' OR 课程编号='IT_C'");
      st.execute("DELETE FROM 学生 WHERE 学号='IT_S'");
      st.execute("DELETE FROM 课程 WHERE 编号='IT_C'");
      st.execute("INSERT INTO 学生(学号,姓名,性别,专业,密码) VALUES('IT_S','测试','男','计算机','pw')");
      st.execute("INSERT INTO 课程(编号,名称,课时,学分,老师,地点,共享) VALUES('IT_C','测试课',32,2,'X','B101','N')");
      c.commit();
    }
  }

  @AfterAll
  static void teardown() throws Exception {
    try (var c = JdbcFactory.fromClasspath("/db.properties").getConnection();
         var st = c.createStatement()) {
      st.execute("DELETE FROM 选课 WHERE 学号='IT_S' OR 课程编号='IT_C'");
      st.execute("DELETE FROM 学生 WHERE 学号='IT_S'");
      st.execute("DELETE FROM 课程 WHERE 编号='IT_C'");
      c.commit();
    }
  }

  @Test @Order(1)
  void enroll_inserts() {
    int rows = dao.enroll("IT_S", "IT_C");
    assertEquals(1, rows);
    assertTrue(dao.exists("IT_S", "IT_C"));
  }

  @Test @Order(2)
  void enroll_duplicate_throws() {
    assertThrows(RuntimeException.class, () -> dao.enroll("IT_S", "IT_C"));
  }

  @Test @Order(3)
  void withdraw_removes() {
    int rows = dao.withdraw("IT_S", "IT_C");
    assertEquals(1, rows);
    assertFalse(dao.exists("IT_S", "IT_C"));
  }
}
```

- [ ] **Step 2: 实现 ChoiceDao**

```java
// college-b/src/main/java/college/b/dao/ChoiceDao.java
package college.b.dao;

import javax.sql.DataSource;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;

public class ChoiceDao {
  public record Row(String courseId, String studentId, String score) {}

  private final DataSource ds;

  public ChoiceDao(DataSource ds) { this.ds = ds; }

  public int enroll(String studentId, String courseId) {
    String sql = "INSERT INTO 选课(课程编号,学号) VALUES(?,?)";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, courseId); ps.setString(2, studentId);
      int rows = ps.executeUpdate();
      c.commit();
      return rows;
    } catch (SQLException e) { throw new RuntimeException("enroll failed", e); }
  }

  public int withdraw(String studentId, String courseId) {
    String sql = "DELETE FROM 选课 WHERE 学号=? AND 课程编号=?";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, studentId); ps.setString(2, courseId);
      int rows = ps.executeUpdate();
      c.commit();
      return rows;
    } catch (SQLException e) { throw new RuntimeException(e); }
  }

  public boolean exists(String studentId, String courseId) {
    String sql = "SELECT 1 FROM 选课 WHERE 学号=? AND 课程编号=?";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, studentId); ps.setString(2, courseId);
      try (var rs = ps.executeQuery()) { return rs.next(); }
    } catch (SQLException e) { throw new RuntimeException(e); }
  }

  public List<Row> findByStudent(String studentId) {
    String sql = "SELECT 课程编号,学号,得分 FROM 选课 WHERE 学号=?";
    try (var c = ds.getConnection(); var ps = c.prepareStatement(sql)) {
      ps.setString(1, studentId);
      try (var rs = ps.executeQuery()) {
        List<Row> out = new ArrayList<>();
        while (rs.next()) out.add(new Row(rs.getString(1), rs.getString(2), rs.getString(3)));
        return out;
      }
    } catch (SQLException e) { throw new RuntimeException(e); }
  }
}
```

- [ ] **Step 3: 运行测试 + commit**

```bash
mvn -pl college-b -am test -Dtest=ChoiceDaoIT
git add college-b/src/
git commit -m "feat(college-b): choice dao for oracle"
```

---

## Task 8: B 院 XSD（studentB / classB / choiceB）

**Files:**
- Create: `college-b/src/main/resources/schema/studentB.xsd`
- Create: `college-b/src/main/resources/schema/classB.xsd`
- Create: `college-b/src/main/resources/schema/choiceB.xsd`
- Test: `college-b/src/test/java/college/b/xml/SchemaBTest.java`

B 院用中文标签，字段名与表列名对齐。

- [ ] **Step 1: 写 XSD**

`studentB.xsd`：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">
  <xs:element name="学生集">
    <xs:complexType>
      <xs:sequence>
        <xs:element name="学生" minOccurs="0" maxOccurs="unbounded">
          <xs:complexType>
            <xs:sequence>
              <xs:element name="学号" type="xs:string"/>
              <xs:element name="姓名" type="xs:string"/>
              <xs:element name="性别" type="xs:string"/>
              <xs:element name="专业" type="xs:string"/>
              <xs:element name="密码" type="xs:string" minOccurs="0"/>
            </xs:sequence>
          </xs:complexType>
        </xs:element>
      </xs:sequence>
    </xs:complexType>
  </xs:element>
</xs:schema>
```

`classB.xsd`：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">
  <xs:element name="课程集">
    <xs:complexType>
      <xs:sequence>
        <xs:element name="课程" minOccurs="0" maxOccurs="unbounded">
          <xs:complexType>
            <xs:sequence>
              <xs:element name="编号" type="xs:string"/>
              <xs:element name="名称" type="xs:string"/>
              <xs:element name="课时" type="xs:integer" minOccurs="0"/>
              <xs:element name="学分" type="xs:decimal"/>
              <xs:element name="老师" type="xs:string"/>
              <xs:element name="地点" type="xs:string"/>
              <xs:element name="共享">
                <xs:simpleType>
                  <xs:restriction base="xs:string">
                    <xs:enumeration value="Y"/><xs:enumeration value="N"/>
                  </xs:restriction>
                </xs:simpleType>
              </xs:element>
            </xs:sequence>
          </xs:complexType>
        </xs:element>
      </xs:sequence>
    </xs:complexType>
  </xs:element>
</xs:schema>
```

`choiceB.xsd`：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">
  <xs:element name="选课集">
    <xs:complexType>
      <xs:sequence>
        <xs:element name="选课" minOccurs="0" maxOccurs="unbounded">
          <xs:complexType>
            <xs:sequence>
              <xs:element name="课程编号" type="xs:string"/>
              <xs:element name="学号" type="xs:string"/>
              <xs:element name="得分" type="xs:string" minOccurs="0"/>
            </xs:sequence>
          </xs:complexType>
        </xs:element>
      </xs:sequence>
    </xs:complexType>
  </xs:element>
</xs:schema>
```

- [ ] **Step 2: 写测试**

```java
// college-b/src/test/java/college/b/xml/SchemaBTest.java
package college.b.xml;

import cn.edu.di.xml.XsdValidator;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SchemaBTest {
  XsdValidator load(String name) { return XsdValidator.fromClasspath("/schema/" + name); }

  @Test void student_valid() {
    String xml = "<学生集><学生><学号>BS001</学号><姓名>张三</姓名><性别>男</性别><专业>计算机</专业></学生></学生集>";
    assertTrue(load("studentB.xsd").validate(xml).valid());
  }

  @Test void class_valid() {
    String xml = "<课程集><课程><编号>BC001</编号><名称>数据库</名称><学分>3</学分><老师>李老师</老师><地点>B101</地点><共享>Y</共享></课程></课程集>";
    assertTrue(load("classB.xsd").validate(xml).valid());
  }

  @Test void choice_valid() {
    String xml = "<选课集><选课><课程编号>BC001</课程编号><学号>BS001</学号></选课></选课集>";
    assertTrue(load("choiceB.xsd").validate(xml).valid());
  }
}
```

- [ ] **Step 3: 运行 + commit**

```bash
mvn -pl college-b -am test -Dtest=SchemaBTest
git add college-b/src/
git commit -m "feat(college-b): local xsds for student/class/choice (oracle)"
```

---

## Task 9: B 院 XML 适配器（StudentB / CourseB / ChoiceB）

**Files:**
- Create: `college-b/src/main/java/college/b/xml/StudentBAdapter.java`
- Create: `college-b/src/main/java/college/b/xml/CourseBAdapter.java`
- Create: `college-b/src/main/java/college/b/xml/ChoiceBAdapter.java`
- Test: `college-b/src/test/java/college/b/xml/StudentBAdapterTest.java`
- Test: `college-b/src/test/java/college/b/xml/CourseBAdapterTest.java`
- Test: `college-b/src/test/java/college/b/xml/ChoiceBAdapterTest.java`

参照 College A 的三个适配器，但 XML 标签用 B 格式（`编号`/`名称`/`老师`/`地点`/`得分` 等）。

三种适配器的 XML 根元素分别是 `学生集`/`课程集`/`选课集`，与 B 的 XSD 对齐。

- [ ] **Step 1: 写三个测试（FAIL）**

```java
// StudentBAdapterTest.java
package college.b.xml;

import college.b.dao.StudentDao;
import cn.edu.di.xml.XmlIO;
import cn.edu.di.xml.XsdValidator;
import org.junit.jupiter.api.Test;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;

class StudentBAdapterTest {
  @Test void marshal_validates_against_xsd() {
    var rows = List.of(new StudentDao.Row("BS001","张三","男","计算机","pw1"));
    var r = XsdValidator.fromClasspath("/schema/studentB.xsd").validate(StudentBAdapter.marshal(rows));
    assertTrue(r.valid(), r.errors().toString());
  }
  @Test void roundtrip() throws Exception {
    var original = List.of(new StudentDao.Row("BS001","张三","男","计算机","pw1"));
    var parsed = StudentBAdapter.unmarshal(XmlIO.parse(StudentBAdapter.marshal(original)));
    assertEquals("张三", parsed.get(0).name());
    assertEquals("计算机", parsed.get(0).major());
  }
}
```

CourseBAdapterTest 和 ChoiceBAdapterTest 同理（参考 Plan 1 Task 14-16 的测试模式，替换为 B 院的 Row 类型和 XSD 路径）。

- [ ] **Step 2: 实现三个适配器**

`StudentBAdapter` marshal 输出 `学生集`/`学生`/`学号`/`姓名`/`性别`/`专业`。`密码` 可选（`minOccurs="0"`）。unmarshal 读同样标签。

`CourseBAdapter` marshal 输出 `课程集`/`课程`/`编号`/`名称`/`课时`/`学分`/`老师`/`地点`/`共享`。

`ChoiceBAdapter` marshal 输出 `选课集`/`选课`/`课程编号`/`学号`/`得分`（可选）。

（实现细节参照 Plan 1 Task 14-16 中 College A 的适配器模式：DocumentHelper.createDocument → addElement → setText → XmlIO.toPrettyString）

- [ ] **Step 3: 运行测试 + commit**

```bash
mvn -pl college-b -am test -Dtest="StudentBAdapterTest,CourseBAdapterTest,ChoiceBAdapterTest"
git add college-b/src/
git commit -m "feat(college-b): xml adapters for student/course/choice (b-format)"
```

---

## Task 10: B 院 Server + Handlers（Login / ListLocal / Enroll / Withdraw）

**Files:**
- Create: `college-b/src/main/java/college/b/server/handler/Handler.java`
- Create: `college-b/src/main/java/college/b/server/handler/LoginHandler.java`
- Create: `college-b/src/main/java/college/b/server/handler/ListLocalCoursesHandler.java`
- Create: `college-b/src/main/java/college/b/server/handler/EnrollLocalHandler.java`
- Create: `college-b/src/main/java/college/b/server/handler/WithdrawLocalHandler.java`
- Create: `college-b/src/main/java/college/b/server/CommandRouter.java`
- Create: `college-b/src/main/java/college/b/server/CollegeBServer.java`
- Test: `college-b/src/test/java/college/b/server/LoginHandlerTest.java`
- Test: `college-b/src/test/java/college/b/server/AcceptLoopTest.java`
- Test: `college-b/src/test/java/college/b/server/handler/CourseHandlersTest.java`

完全参照 College A 的 server 代码（Plan 1 Task 17-18），唯一差异：

1. 包名 `college.b.*`（不是 `college.a.*`）
2. LoginHandler 的 payload XML 解析 → 匹配 B 的 AuthService.Session（role 来自 level 映射）
3. ListLocalCoursesHandler 调用 `CourseBAdapter.marshal()`（不是 CourseAAdapter）
4. EnrollLocalHandler 调用 `choiceDao.enroll()`（不是 `enrollLocal`）
5. CollegeBServer.main() 默认端口 9002，命令路由注册用 B 院的 DAO/Service

- [ ] **Step 1: 写测试（FAIL）→ 实现 → PASS → commit**

```bash
mvn -pl college-b -am test -Dtest="LoginHandlerTest,AcceptLoopTest,CourseHandlersTest"
git add college-b/src/
git commit -m "feat(college-b): server skeleton with all local handlers"
```

---

## Task 11: B 院种子数据生成器

**Files:**
- Create/modify: `scripts/seed-data/src/main/java/seed/SeedB.java`
- Test: `scripts/seed-data/src/test/java/seed/SeedBGeneratorTest.java`

生成 `college-b/src/main/resources/sql/init_b_data.sql`。

- [ ] **Step 1: 写测试 + 实现 SeedB**

50 学生（BS001-BS050，中文随机姓名），10 课程（BC001-BC010，前 4 门 share='Y'），每生 5 选课（共 250）。账户 51 个（每个学生一个 + admin 一个）。

B 院用 level 表示角色：学生 level=1，admin level=5。学生密码 = `pw` + 编号（如 `pw001`）。

toSql 格式适配 Oracle 语法（注意 `NUMBER` 类型不需要单引号，`COMMIT` 每批后加）。

- [ ] **Step 2: 运行测试 + 生成 + 灌入**

```bash
mvn -pl scripts/seed-data -am test -Dtest="SeedBGeneratorTest"
mvn -pl scripts/seed-data exec:java -Dexec.mainClass=seed.SeedB -Dexec.args="42"
docker exec -i di-oracle sqlplus -s collegeb/collegeb@freepdb1 < college-b/src/main/resources/sql/init_b_data.sql
```

- [ ] **Step 3: commit**

```bash
git add scripts/seed-data/ college-b/src/main/resources/sql/init_b_data.sql
git commit -m "feat(seed): generator for college b (50/10/250 oracle)"
```

---

## Task 12: MySQL Docker 启动脚本 + C 院 init_c.sql

**Files:**
- Create: `scripts/db/start-mysql.sh`
- Create: `college-c/src/main/resources/sql/init_c.sql`
- Create: `college-c/src/main/resources/db.properties`

C 院 MySQL 表结构（按 design spec §4.1）：

| 表 | 字段 |
|---|---|
| 账户 | `acc varchar(12)` PK / `passwd varchar(12)` / `CreateDate timestamp` |
| 学生 | `Sno char(12)` PK / `Snm varchar(10)` / `Sex char(2)` / `Sde varchar(10)` / `Pwd varchar(12)` |
| 课程 | `Cno char(4)` PK / `Cnm varchar(20)` / `Ctm int` / `Cpt decimal(3,1)` / `Tec varchar(10)` / `Pla varchar(20)` / `Share char(1)` |
| 选课 | `Cno char(4)` FK / `Sno char(12)` FK / `Grd varchar(3)` |

- [ ] **Step 1: 写 MySQL Docker 启动脚本**

```bash
#!/usr/bin/env bash
# scripts/db/start-mysql.sh
set -e
NAME=di-mysql
PASSWORD='mysql123'

if docker ps -a --format '{{.Names}}' | grep -q "^${NAME}$"; then
  docker start "${NAME}"
else
  docker run -e "MYSQL_ROOT_PASSWORD=${PASSWORD}" \
    -p 3306:3306 --name "${NAME}" \
    -d mysql:8.0
fi

echo "Waiting for MySQL..."
for i in {1..30}; do
  docker exec "${NAME}" mysql -uroot -p"${PASSWORD}" -e "SELECT 1" >/dev/null 2>&1 && break
  sleep 2
done
echo "MySQL up at localhost:3306 (root / ${PASSWORD})"
```

```bash
chmod +x scripts/db/start-mysql.sh
```

- [ ] **Step 2: 写 init_c.sql**

```sql
-- college-c/src/main/resources/sql/init_c.sql
CREATE DATABASE IF NOT EXISTS collegeC CHARACTER SET utf8mb4;
USE collegeC;

DROP TABLE IF EXISTS 选课;
DROP TABLE IF EXISTS 学生;
DROP TABLE IF EXISTS 课程;
DROP TABLE IF EXISTS 账户;

CREATE TABLE 账户 (
  acc        varchar(12)  NOT NULL PRIMARY KEY,
  passwd     varchar(12)  NOT NULL,
  CreateDate timestamp    NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;

CREATE TABLE 学生 (
  Sno char(12)    NOT NULL PRIMARY KEY,
  Snm varchar(10) NOT NULL,
  Sex char(2)     NOT NULL,
  Sde varchar(10) NOT NULL,
  Pwd varchar(12) NOT NULL
) ENGINE=InnoDB;

CREATE TABLE 课程 (
  Cno   char(4)       NOT NULL PRIMARY KEY,
  Cnm   varchar(20)   NOT NULL,
  Ctm   int           NOT NULL DEFAULT 32,
  Cpt   decimal(3,1)  NOT NULL,
  Tec   varchar(10)   NOT NULL,
  Pla   varchar(20)   NOT NULL,
  Share char(1)       NOT NULL
) ENGINE=InnoDB;

CREATE TABLE 选课 (
  Cno char(4)    NOT NULL,
  Sno char(12)   NOT NULL,
  Grd varchar(3) NULL,
  FOREIGN KEY (Cno) REFERENCES 课程(Cno),
  FOREIGN KEY (Sno) REFERENCES 学生(Sno),
  UNIQUE KEY UK_SC (Sno, Cno)
) ENGINE=InnoDB;
```

- [ ] **Step 3: 写 db.properties 并更新 college-c/pom.xml（加 mysql-connector-j）**

`db.properties`:
```properties
jdbc.url=jdbc:mysql://localhost:3306/collegeC?useSSL=false&allowPublicKeyRetrieval=true
jdbc.user=root
jdbc.password=mysql123
```

pom.xml 加：
```xml
<dependency>
  <groupId>com.mysql</groupId>
  <artifactId>mysql-connector-j</artifactId>
  <version>8.3.0</version>
</dependency>
```

- [ ] **Step 4: 启动 MySQL 并建库**

```bash
./scripts/db/start-mysql.sh
docker exec -i di-mysql mysql -uroot -pmysql123 < college-c/src/main/resources/sql/init_c.sql
```

- [ ] **Step 5: commit**

```bash
git add scripts/db/start-mysql.sh college-c/src/main/resources/ college-c/pom.xml pom.xml
git commit -m "build(college-c): mysql docker script, init schema, and jdbc driver"
```

---

## Task 13-17: C 院的 JDBC + DAO + Auth + XSD + Adapter + Server

这些 Task 与 B 院完全对应的模式，差异：

- 包名 `college.c.*`
- 表/列名用 C 院命名（`Sno`/`Snm`/`Sex`/`Sde`/`Pwd` / `Cno`/`Cnm`/`Ctm`/`Cpt`/`Tec`/`Pla`/`Share` / `acc`/`passwd`/`CreateDate` / `Grd`）
- MySQL 方言：自增用 `AUTO_INCREMENT`，UPSERT 用 `INSERT IGNORE`，连接池不必手动 commit（JDBC autocommit）
- AuthService：C 院账户表无 role 字段 → 统一返回 `"stu"`（或从 `CreateDate` 推 admin，本次不判断）
- 端口：CollegeCServer 默认 9003
- 学号前缀 `CS`，课程号前缀 `CC`

**Task 13:** JdbcFactory + AccountDao  
**Task 14:** AuthService  
**Task 15:** StudentDao + CourseDao + ChoiceDao  
**Task 16:** C 院 XSD（studentC / classC / choiceC）+ SchemaCTest  
**Task 17:** C 院 Adapter + Server + Handlers + Tests  
**Task 18:** C 院种子数据（SeedC.java）

每个 Task 都遵循 TDD（先写测试→确认 FAIL→实现→PASS→commit）。

---

## Task 19: XSL 转换文件（12 个）

**Files (全部创建在 `integration/src/main/resources/xsl/`):**
- `formatA.xsl` — A 格式 → 统一格式（课程）
- `formatB.xsl` — B 格式 → 统一格式（课程）
- `formatC.xsl` — C 格式 → 统一格式（课程）
- `AtoB.xsl`, `AtoC.xsl` — A 格式 → B/C 格式（课程）
- `BtoA.xsl`, `BtoC.xsl` — B 格式 → A/C 格式（课程）
- `CtoA.xsl`, `CtoB.xsl` — C 格式 → A/B 格式（课程）
- `studentFormat.xsl` — 通用学生格式转换模板
- `choiceFormat.xsl` — 通用选课格式转换模板
- `identity.xsl` — 恒等转换（DEBUG 用）

> 说明：三个 formatX.xsl 把院系格式课程 XML → 统一格式（id/name/time/score/teacher/location/share/origin）。六个 XtoY.xsl 做反向转换。studentFormat.xsl 和 choiceFormat.xsl 处理学生和选课的双向转换（可合并逻辑以减少文件数）。

- [ ] **Step 1: 写 formatA.xsl**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<xsl:stylesheet version="1.0" xmlns:xsl="http://www.w3.org/1999/XSL/Transform">
  <xsl:output method="xml" encoding="UTF-8" indent="yes"/>
  <xsl:template match="课程集">
    <classes>
      <xsl:for-each select="课程">
        <class>
          <id><xsl:value-of select="课程编号"/></id>
          <name><xsl:value-of select="课程名称"/></name>
          <time><xsl:value-of select="课时"/></time>
          <score><xsl:value-of select="学分"/></score>
          <teacher><xsl:value-of select="授课老师"/></teacher>
          <location><xsl:value-of select="授课地点"/></location>
          <share><xsl:value-of select="共享"/></share>
          <origin>A</origin>
        </class>
      </xsl:for-each>
    </classes>
  </xsl:template>
</xsl:stylesheet>
```

formatB.xsl 与 formatC.xsl 同理，映射各自的列名 → 统一标签，origin 分别设为 B 和 C。

- [ ] **Step 2: 写 AtoB.xsl 示例（反向转换）**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<xsl:stylesheet version="1.0" xmlns:xsl="http://www.w3.org/1999/XSL/Transform">
  <xsl:output method="xml" encoding="UTF-8" indent="yes"/>
  <xsl:template match="classes">
    <课程集>
      <xsl:for-each select="class">
        <课程>
          <编号><xsl:value-of select="id"/></编号>
          <名称><xsl:value-of select="name"/></名称>
          <课时><xsl:value-of select="time"/></课时>
          <学分><xsl:value-of select="score"/></学分>
          <老师><xsl:value-of select="teacher"/></老师>
          <地点><xsl:value-of select="location"/></地点>
          <共享><xsl:value-of select="share"/></共享>
        </课程>
      </xsl:for-each>
    </课程集>
  </xsl:template>
</xsl:stylesheet>
```

其余 4 个 XtoY.xsl 同理（BtoA/CtoA/CtoB 等）。

- [ ] **Step 3: 写 studentFormat.xsl**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<xsl:stylesheet version="1.0" xmlns:xsl="http://www.w3.org/1999/XSL/Transform">
  <xsl:output method="xml" encoding="UTF-8" indent="yes"/>
  <xsl:template match="学生集">
    <students>
      <xsl:for-each select="学生">
        <student>
          <id><xsl:value-of select="学号"/></id>
          <name><xsl:value-of select="姓名"/></name>
          <sex><xsl:value-of select="性别"/></sex>
          <major><xsl:value-of select="专业|院系|Sde"/></major>
          <origin><xsl:value-of select="'B'"/></origin>
        </student>
      </xsl:for-each>
    </students>
  </xsl:template>
</xsl:stylesheet>
```

（注：`专业|院系|Sde` 用 XPath union 处理三院字段名差异）

- [ ] **Step 4: 写 XSL 单元测试（验证每个 .xsl 转换正确）**

```java
// integration/src/test/java/integration/transform/XslTest.java
package integration.transform;

import cn.edu.di.xml.XsltTransformer;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class XslTest {
  @Test void formatA_converts_course_to_unified() {
    var t = XsltTransformer.fromClasspath("/xsl/formatA.xsl");
    String in = "<课程集><课程><课程编号>AC001</课程编号><课程名称>DB</课程名称><课时>32</课时><学分>3</学分><授课老师>Li</授课老师><授课地点>A101</授课地点><共享>Y</共享></课程></课程集>";
    String out = t.transform(in);
    assertTrue(out.contains("<id>AC001</id>"));
    assertTrue(out.contains("<origin>A</origin>"));
  }

  @Test void AtoB_roundtrip_roughly() {
    var toUnified = XsltTransformer.fromClasspath("/xsl/formatA.xsl");
    var toB = XsltTransformer.fromClasspath("/xsl/AtoB.xsl");
    String in = "<课程集><课程><课程编号>AC001</课程编号><课程名称>DB</课程名称><课时>32</课时><学分>3</学分><授课老师>Li</授课老师><授课地点>A101</授课地点><共享>Y</共享></课程></课程集>";
    String unified = toUnified.transform(in);
    String bFormat = toB.transform(unified);
    assertTrue(bFormat.contains("<编号>AC001</编号>"));
    assertTrue(bFormat.contains("<老师>Li</老师>"));
  }
  // ... additional tests for formatB, formatC, BtoA, CtoA, etc.
}
```

- [ ] **Step 5: 运行测试 + commit**

```bash
mvn -pl integration -am test -Dtest=XslTest
git add integration/src/
git commit -m "feat(integration): 12 xsl transformation files with tests"
```

---

## Task 20: 客户端支持 B/C 学院

**Files:**
- Modify: `client/src/main/java/client/ui/LoginFrame.java`
- Modify: `client/src/main/java/client/ui/CourseListFrame.java`

客户端 `--college=B` 或 `--college=C` 时，LoginFrame 标题和 CourseListFrame 标题显示对应学院名。同时 CourseListFrame 的 XML 解析需要适配 B/C 的标签名差异（`编号` vs `课程编号`，`老师` vs `授课老师` 等）。

- [ ] **Step 1: 修改 LoginFrame/CourseListFrame 支持 college 参数**

使 CourseListFrame 根据 college 参数选择正确的 XML 标签名来解析课程列表。例如：

```java
// 在 CourseListFrame 中
String tagCourse = "课程";
String tagId = college.equals("B") ? "编号" : "课程编号";
String tagName = college.equals("C") ? "Cnm" :
                 college.equals("B") ? "名称" : "课程名称";
// ...
```

（具体适配逻辑在执行时根据实际 B/C 返回的 XML 格式调整）

- [ ] **Step 2: 验证编译 + commit**

```bash
mvn -q -DskipTests compile
git add client/
git commit -m "feat(client): support --college=B and --college=C"
```

---

## Task 21: start-all.sh 整合 B + C

**Files:**
- Modify: `scripts/start-all.sh`

在启动脚本中加入：
1. 启动 Oracle（`start-oracle.sh`）和 MySQL（`start-mysql.sh`）
2. 构建 B 和 C 的 classpath（`dependency:build-classpath`）
3. 启动 CollegeBServer（port 9002）和 CollegeCServer（port 9003）
4. 更新客户端启动提示（支持 `--college=B --server=127.0.0.1:9002` 和 `--college=C --server=127.0.0.1:9003`）

- [ ] **Step 1: 更新 start-all.sh**

```bash
# 在 [1/3] 步骤中加入
./scripts/db/start-oracle.sh
./scripts/db/start-mysql.sh

# 在 [3/3] 步骤中加入 B 和 C 的 classpath 生成和启动
mvn -q dependency:build-classpath -Dmdep.outputFile=target/classpath.txt -pl college-b
mvn -q dependency:build-classpath -Dmdep.outputFile=target/classpath.txt -pl college-c

java -Dport=9002 -cp college-b/target/classes:common/target/classes:$(cat college-b/target/classpath.txt) \
    college.b.server.CollegeBServer >logs/college-b.log 2>&1 &
echo $! > logs/college-b.pid

java -Dport=9003 -cp college-c/target/classes:common/target/classes:$(cat college-c/target/classpath.txt) \
    college.c.server.CollegeCServer >logs/college-c.log 2>&1 &
echo $! > logs/college-c.pid
```

- [ ] **Step 2: commit**

```bash
git add scripts/start-all.sh
git commit -m "build: add college-b and college-c to start-all.sh"
```

---

## Plan 2 验收

完成本计划后应满足：

- Oracle（B）和 MySQL（C）Docker 容器正常启动
- B/C 各自的 Server 可启动，客户端可登录（`--college=B` / `--college=C`）
- 客户端可查 B/C 本院课程列表；可本院选课/退课
- 12 个 XSL 文件存在且至少 format 方向的转换测试通过
- `mvn test` 全部通过
- `./scripts/start-all.sh` 能启动 1 个 Integration Server + 3 个 College Server（A/B/C）

**下一步：Plan 3 — 跨院流程（课程共享、跨院选课、跨院退课、全局统计）+ Integration Server 路由逻辑 + 客户端跨院 UI + demo 脚本。**
